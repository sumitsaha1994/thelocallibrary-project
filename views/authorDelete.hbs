<h1>{{title}}: {{author.name}}</h1>
<p>{{author.lifespan}}</p>
{{#if author_books.length}}
<p><strong>Delete the following books before attempting to delete this author.</strong></p>
<div style='margin-left:20px;margin-top:20px'>
    <h4>Books</h4>
    <dl>
        {{#each author_books}}
        <dt><a href={{url}}>{{title}}</a></dt>
        <dd>{{summary}}</dd>
        {{/each}}
    </dl>
</div>
{{else}}
<p>Do you really want to delete this Author?</p>
<form action="" method="POST">
    <div class="form-group">
        <input type="hidden" name="authorid" id="authorid" class="form-control" value={{author._id}} required>
        <button class="btn btn-primary" type="submit">Delete</button>
    </div>
</form>
{{/if}}