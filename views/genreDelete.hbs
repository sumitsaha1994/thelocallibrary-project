<h1>Delete Genre: {{genre.name}}</h1>
{{#if genre_books.length}}
<p><strong>Delete the following books before attempting to delete this genre.</strong></p>
<div style='margin-left:20px;margin-top:20px'>
    <h4>Books</h4>
    {{#each genre_books}}
    <dt>
        <a href={{url}}>{{title}}</a>
    </dt>
    <dd>{{summary}}</dd>
    {{/each}}
</div>
{{else}}
<p>Do you really want to delete this Genre?</p>
<form action="" method="POST">
    <div class="form-group">
        <input type="hidden" id="id" name="id" class="form-control" value={{genre._id}} required>
    </div>
    <button class="btn btn-primary">Delete</button>
</form>
{{/if}}